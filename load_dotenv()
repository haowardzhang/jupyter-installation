默认查找逻辑
调用 load_dotenv() 不带参数时，内部使用 find_dotenv() 来定位文件：

从调用脚本所在的文件位置开始（实际上是从当前工作目录 / 调用栈的文件位置）
逐级向上遍历父目录查找名为 .env 的文件
找到第一个就停止；找不到返回空字符串（不报错）

关键细节
行为	说明
向上递归查找	不只是当前目录，会一直往父目录找（usecwd 参数影响起点）
找不到文件	不报错，静默返回 False
已存在的环境变量	默认不覆盖（override=False）
起点	默认基于调用栈帧的文件路径，不一定是 os.getcwd()
起点的坑
默认起点是调用 load_dotenv() 的那个 .py 文件所在目录，而不是严格的当前工作目录。如果你想明确以"当前工作目录"为起点，需要：


指定明确路径
如果想避免歧义，最好显式传入路径：


总结：load_dotenv() 缺省会查找 .env，但不是"只在当前路径"——它会从调用文件位置开始向上递归查找父目录，找不到也不会报错
